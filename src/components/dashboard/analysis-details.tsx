import type { AnalyzeResponse } from "@/features/market-analysis/model";

export function AnalysisDetails({ data }: { data: AnalyzeResponse }) { return <><section className="detail-grid"><article className="panel"><p className="panel-label">支持理由</p><ul>{data.decision.reasons.map((reason) => <li key={reason}>{reason}</li>)}</ul></article><article className="panel warning"><p className="panel-label">風險提示</p><ul>{[...data.completenessWarnings,...data.decision.riskWarnings].map((warning,index) => <li key={`${warning}-${index}`}>{warning}</li>)}</ul></article></section><footer className="analysis-meta"><span>Fixture：{data.snapshot.fixtureVersion}</span><span>擷取：{new Date(data.snapshot.sourceRequestTime).toLocaleString("zh-TW")}</span><span>最後收盤：{new Date(data.snapshot.lastClosedCandleAt).toLocaleString("zh-TW")}</span></footer></>; }
