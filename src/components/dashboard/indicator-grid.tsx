import type { AnalyzeResponse } from "@/features/market-analysis/model";

export function IndicatorGrid({ data }: { data: AnalyzeResponse }) { const s = data.decision.categorySignals; const i = data.snapshot.indicators; const cards = [["TREND",s.trend,`EMA20 ${i.ema20.toFixed(0)}`],["MOMENTUM",s.momentum,`RSI ${i.rsi14.toFixed(1)}`],["PARTICIPATION",s.participation,`Volume ${i.volumeChangePct.toFixed(1)}%`],["CROWDING",s.crowding,`Funding ${((i.fundingRate ?? 0)*100).toFixed(4)}%`]] as const; return <section className="indicator-grid">{cards.map(([label,value,detail]) => <article className="metric" key={label}><p className="panel-label">{label}</p><strong className={value > 0 ? "positive" : value < 0 ? "negative" : "neutral"}>{value > 0 ? "+1" : value}</strong><small>{detail}</small></article>)}</section>; }
